Clerk Demo for Tide.

run `npm run dev` to start the app.

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

.env.local is replaced with placeholder: [.env.example](.env.example), create a new Clerk app and place your keys in the example file.


